services:
  web:
    command: npm run dev -- --host 0.0.0.0 --port 3000
    environment:
      VITE_USE_POLLING: 'true'
    volumes:
      - ./apps/web:/app
      - web_node_modules:/app/node_modules

volumes:
  web_node_modules:
